[alias]
# Override a global incremental=false only for these development commands.
dev       = ["--config", "build.incremental=true", "run"]
dev-build = ["--config", "build", "build.incremental=true"]
